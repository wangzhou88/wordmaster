# 🔧 Git 安装指南

## 🚀 快速安装步骤

### 1. 下载Git
1. 访问：https://git-scm.com/download/win
2. 下载Windows版本（通常自动开始下载）
3. 运行安装程序

### 2. 安装选项（推荐设置）
- ✅ 添加Git到PATH
- ✅ Git LFS支持
- ✅ 默认文本编辑器：Use Visual Studio Code as Git's default editor（如果已安装VS Code）
- ✅ HTTPS传输：Use the OpenSSL library
- ✅ Line ending：Checkout Windows-style, commit Unix-style
- ✅ Terminal：Use Windows' default console window
- ✅ Extra options：Enable file system caching, Enable Git Credential Manager

### 3. 验证安装
安装完成后，打开新的命令行窗口，输入：
```bash
git --version
```
如果显示版本号，说明安装成功。

## ⚡ 一键安装命令（可选）

如果您有管理员权限，也可以使用Chocolatey：
```bash
choco install git
```

## 🔄 安装后继续APK构建

安装完Git后，请告诉我：
1. Git版本信息
2. 您的GitHub用户名

然后我将立即帮您推送代码并启动APK构建！

---
**Git安装完成后，请说"完成"或提供您的GitHub用户名。**